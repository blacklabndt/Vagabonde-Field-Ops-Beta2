{
  "name": "vagabonde-field-ops-beta2",
  "private": true,
  "description": "Repo-root build entry. The app itself lives in vite-app/.",
  "scripts": {
    "//": "Cloudflare Workers Builds runs from the repo root with no root-directory setting, so the build reaches down into the app. `npm ci` rather than `install` so the lockfile is honoured exactly and a CI build can't quietly pick up a different dependency version than the one tested here.",
    "build": "npm --prefix vite-app ci && npm --prefix vite-app run build",
    "test": "npm --prefix vite-app test"
  }
}
