{
  "name": "vagabonde-field-ops-beta2",
  "private": true,
  "description": "Repo-root build entry. The app itself lives in vite-app/.",
  "scripts": {
    "//": "Repo-root build entry; install the locked app dependencies before building.",
    "build": "npm --prefix vite-app ci && npm --prefix vite-app run build",
    "lint": "npm --prefix vite-app run lint",
    "typecheck": "npm --prefix vite-app run typecheck",
    "test": "npm --prefix vite-app test",
    "//deploy": "Local deployment runs tests and build first. GitHub Actions automatically deploys the Worker and SPA after successful checks on room/37dbe6165f-beta-2-review. Supabase functions and migrations release separately.",
    "deploy": "npm test && npm run build && npx wrangler deploy"
  }
}
